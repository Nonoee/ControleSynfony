<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
        
        <a class="navbar-brand" href="{{path('home')}}">Controle</a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav me-auto">
            <li class="nav-item">
                <a class="nav-link active" href="{{path('home')}}">Acceuil</a>
            </li>
            <li class="nav-item">
                <a class="nav-link active" href="{{path('contact_index')}}">Contact liste</a>
            </li>
            <li class="nav-item">
                <a class="nav-link active" href="{{path('contact_create')}}">Crée un contact</a>
            </li>
        </ul>
        </div>
    </div>
</nav>

{% for label, messages in app.flashes %}{# label aura success, danger,.. Message aura les messages #}
<div class="container">
    <div class="alert-{{ label }}">{# ca met la couleur de fond en fonction du label donc saccess, warning,... #}
        {% for message in messages %}
            <p>{{ message | raw }}</p>{# le raw enleve les balises qui sont proteger par twig, ici ca met en gras #}
        {% endfor %}
    </div>
</div>
{% endfor %}